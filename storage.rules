rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Apenas arquivos na raiz (sem subpastas)
    match /{file} {
      allow read, write: if true;
    }
    match /uploads/{file} {
      allow read, write: if true;
    }
  }
}